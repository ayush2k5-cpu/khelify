name: khelify_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI & Icons
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  lucide_icons: ^0.257.0
  phosphor_flutter: ^2.0.1

  # Firebase
  firebase_auth: ^4.17.6
  cloud_firestore: ^4.15.6
  firebase_storage: ^11.2.0

  # HTTP & Networking
  http: ^1.1.0

  # Camera & Media - UPDATED FOR REAL CAMERA
  camera: ^0.10.5+9
  image_picker: ^1.0.4
  video_player: ^2.8.1
  path_provider: ^2.1.1
  permission_handler: ^11.0.1

  # Caching & Misc
  cached_network_image: ^3.3.1
  google_maps_flutter: ^2.4.0
  modal_bottom_sheet: ^3.0.0
  percent_indicator: ^4.2.2
  fl_chart: ^0.60.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
